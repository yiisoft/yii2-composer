dist: trusty

language: php

matrix:
  include:
    - php: 5.4
      env: COMPOSER_VERSION=1.9.*
    - php: 5.4
      env: COMPOSER_VERSION=1.10.*
    - php: 5.4
      env: COMPOSER_VERSION=^2.0@dev composer/semver:^2.0@dev

    - php: 5.5
      env: COMPOSER_VERSION=1.9.*
    - php: 5.5
      env: COMPOSER_VERSION=1.10.*
    - php: 5.5
      env: COMPOSER_VERSION=^2.0@dev composer/semver:^2.0@dev

    - php: 5.6
      env: COMPOSER_VERSION=1.9.*
    - php: 5.6
      env: COMPOSER_VERSION=1.10.*
    - php: 5.6
      env: COMPOSER_VERSION=^2.0@dev composer/semver:^2.0@dev

    - php: 7.0
      env: COMPOSER_VERSION=1.9.*
    - php: 7.0
      env: COMPOSER_VERSION=1.10.*
    - php: 7.0
      env: COMPOSER_VERSION=^2.0@dev composer/semver:^2.0@dev

    - php: 7.1
      env: COMPOSER_VERSION=1.9.*
    - php: 7.1
      env: COMPOSER_VERSION=1.10.*
    - php: 7.1
      env: COMPOSER_VERSION=^2.0@dev composer/semver:^2.0@dev

    - php: 7.2
      env: COMPOSER_VERSION=1.9.*
    - php: 7.2
      env: COMPOSER_VERSION=1.10.*
    - php: 7.2
      env: COMPOSER_VERSION=^2.0@dev composer/semver:^2.0@dev

    - php: 7.3
      env: COMPOSER_VERSION=1.9.*
    - php: 7.3
      env: COMPOSER_VERSION=1.10.*
    - php: 7.3
      env: COMPOSER_VERSION=^2.0@dev composer/semver:^2.0@dev

    - php: 7.4
      env: COMPOSER_VERSION=1.9.*
    - php: 7.4
      env: COMPOSER_VERSION=1.10.*
    - php: 7.4
      env: COMPOSER_VERSION=^2.0@dev composer/semver:^2.0@dev

    - php: nightly
      env: COMPOSER_VERSION=^2.0@dev composer/semver:^2.0@dev

  allow_failures:
    - php: nightly

sudo: false

# cache vendor dirs
cache:
  directories:
    - $HOME/.composer/cache
    - $HOME/.bin

install:
  - travis_retry composer self-update && composer --version
  - export PATH="$HOME/.composer/vendor/bin:$PATH"
  - travis_retry composer install --prefer-dist --no-interaction

before_script:
  - |
    if [ $TRAVIS_PHP_VERSION = '5.6' ]; then
      PHPUNIT_FLAGS="--coverage-clover=clover.xml"
    else
      phpenv config-rm xdebug.ini || return 0
    fi
  - composer require --dev "composer/composer:"$COMPOSER_VERSION --update-with-dependencies

script:
  - ./vendor/bin/phpunit --verbose $PHPUNIT_FLAGS
  - cd tests/scripts && sh run.sh
